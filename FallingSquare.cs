using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Four_in_a_Row
{
    class FallingSquare
    {
        //Local variables
        int owner;
        int column;
        int targetrow;
        int speed;
        int currentheight;
        int size;

        //Constructor
        public FallingSquare(int Owner, int Column, int TargetRow, int Speed, int Size)
        {
            owner = Owner;
            column = Column;
            targetrow = TargetRow;
            speed = Speed;
            size = Size;
            //currentheight = 0;
        }

        //Properties

    }
}
